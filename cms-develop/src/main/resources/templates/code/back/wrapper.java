package %s.wrapper;

import org.springcms.core.mybatis.base.CmsXBaseWrapper;
import %s.entity.%s;

public class %sWrapper extends CmsXBaseWrapper<%s> {

    public %sWrapper(%s entity) {
        super(entity);
    }
}
